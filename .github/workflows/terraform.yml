name: Terraform Plan & Apply

on:
  pull_request:
    branches: [main]
    paths:
      - 'free-tier/**'
      - 'wordpress-infra/**'
      - 'ghost-infra/**'
      - '.github/workflows/terraform.yml'
  push:
    branches: [main]
    paths:
      - 'free-tier/**'
      - 'wordpress-infra/**'
      - 'ghost-infra/**'

env:
  AWS_REGION: us-east-2
  TOFU_VERSION: 1.6.0

jobs:
  # Detect which modules changed
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      free-tier: ${{ steps.changes.outputs.free-tier }}
      wordpress-infra: ${{ steps.changes.outputs.wordpress-infra }}
      ghost-infra: ${{ steps.changes.outputs.ghost-infra }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Detect module changes
        id: changes
        run: |
          if git diff --name-only origin/main..HEAD | grep -q "free-tier/"; then
            echo "free-tier=true" >> $GITHUB_OUTPUT
          else
            echo "free-tier=false" >> $GITHUB_OUTPUT
          fi
          
          if git diff --name-only origin/main..HEAD | grep -q "wordpress-infra/"; then
            echo "wordpress-infra=true" >> $GITHUB_OUTPUT
          else
            echo "wordpress-infra=false" >> $GITHUB_OUTPUT
          fi
          
          if git diff --name-only origin/main..HEAD | grep -q "ghost-infra/"; then
            echo "ghost-infra=true" >> $GITHUB_OUTPUT
          else
            echo "ghost-infra=false" >> $GITHUB_OUTPUT
          fi

  # Plan stage - runs on PR
  plan:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    needs: detect-changes
    
    strategy:
      matrix:
        module: [free-tier, wordpress-infra, ghost-infra]
      max-parallel: 3
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: ${{ env.TOFU_VERSION }}
      
      - name: Cache Terraform plugins
        uses: actions/cache@v3
        with:
          path: ~/.local/share/opentofu/plugins
          key: ${{ runner.os }}-opentofu-${{ hashFiles('**/.terraform.lock.hcl') }}
          restore-keys: |
            ${{ runner.os }}-opentofu-
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Terraform Init - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          tofu init \
            -backend-config="bucket=tfstate-0001x" \
            -backend-config="key=${{ matrix.module }}/terraform.tfstate" \
            -backend-config="dynamodb_table=terraform-locks" \
            -backend-config="region=${{ env.AWS_REGION }}" \
            -upgrade
      
      - name: Terraform Validate - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: tofu validate
      
      - name: Terraform Format Check - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: tofu fmt -check -recursive
        continue-on-error: true
      
      - name: Terraform Plan - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        env:
          TF_VAR_vault_token: ${{ secrets.VAULT_TOKEN }}
        run: |
          tofu plan \
            -lock-timeout=10m \
            -out=tfplan \
            -no-color | tee plan.txt
      
      - name: Summarize Plan
        working-directory: ${{ matrix.module }}
        run: |
          echo "## 📋 Terraform Plan - ${{ matrix.module }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat plan.txt | tail -20 >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
      
      - name: Cost Estimation Check
        working-directory: ${{ matrix.module }}
        run: |
          # Basic cost warning for RDS
          if grep -q "aws_db_instance" <<< "$(tofu show tfplan)"; then
            echo "⚠️ RDS instance detected - ensure free tier eligible (db.t3.micro)"
          fi
        continue-on-error: true

  # Apply stage - runs on merge to main
  apply:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: detect-changes
    environment:
      name: production
    
    strategy:
      matrix:
        module: [free-tier, wordpress-infra, ghost-infra]
      max-parallel: 1  # Sequential to prevent race conditions
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: ${{ env.TOFU_VERSION }}
      
      - name: Cache Terraform plugins
        uses: actions/cache@v3
        with:
          path: ~/.local/share/opentofu/plugins
          key: ${{ runner.os }}-opentofu-${{ hashFiles('**/.terraform.lock.hcl') }}
          restore-keys: |
            ${{ runner.os }}-opentofu-
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Terraform Init - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          tofu init \
            -backend-config="bucket=tfstate-0001x" \
            -backend-config="key=${{ matrix.module }}/terraform.tfstate" \
            -backend-config="dynamodb_table=terraform-locks" \
            -backend-config="region=${{ env.AWS_REGION }}"
      
      - name: Terraform Apply - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        env:
          TF_VAR_vault_token: ${{ secrets.VAULT_TOKEN }}
        run: |
          tofu apply \
            -auto-approve \
            -lock-timeout=10m \
            -lock=true
      
      - name: Export Outputs - ${{ matrix.module }}
        working-directory: ${{ matrix.module }}
        run: |
          echo "## ✅ Applied: ${{ matrix.module }}" >> $GITHUB_STEP_SUMMARY
          tofu output -json > outputs.json
          cat outputs.json >> $GITHUB_STEP_SUMMARY
      
      - name: Post to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ Terraform applied successfully'
            })

  tflint:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    strategy:
      matrix:
        module: [free-tier, wordpress-infra]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v3
      
      - name: Init TFLint
        working-directory: ${{ matrix.module }}
        run: tflint --init
      
      - name: Run TFLint
        working-directory: ${{ matrix.module }}
        run: tflint -f compact
        continue-on-error: true

  summary:
    runs-on: ubuntu-latest
    name: Status Summary
    if: always()
    needs: [plan, apply]
    
    steps:
      - name: ✅ All infrastructure checks passed
        if: success()
        run: |
          echo "✅ Terraform validation & deployment successful"
          echo ""
          echo "Summary:"
          echo "- All modules validated"
          echo "- Plans reviewed"
          echo "- Infrastructure deployed"
      
      - name: ⚠️ Review infrastructure changes
        if: failure()
        run: |
          echo "⚠️ Infrastructure deployment failed - review logs above"
          exit 1
